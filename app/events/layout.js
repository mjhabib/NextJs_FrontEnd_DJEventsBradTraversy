export const metadata = {
  title: "Events Page",
  description: "Events Page, Instructed by Brad Traversy",
  keywords: "music, dj, edm, events",
};

export default function EventsLayout({children}) {
  return <>{children}</>;
}
