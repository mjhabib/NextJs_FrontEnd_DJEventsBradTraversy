// 'use client';
// import { useRouter } from "next/navigation";

export default function AddEventPage({params}) {

    // const router = useRouter();
    // console.log(router);

    return (
      <div>
        <h1>Add Event Page {params.slug}</h1>
      </div>
    );
  }
  